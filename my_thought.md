目标：设计一套各模块独立的菜单系统，并接入wifi模块作为开源示例/测试

思路：既然要独立，那就得设计通信结构
    与此同时必须分工明确S
    将整套系统分为三个模块
    1.菜单逻辑/显示模块//所有的显示必须调用它
    2.按键模块          //只有它可以返回键值结构
    3.功能模块（如wifi）//可以介入到菜单模块中，并且用简单的适配就能调用菜单API显示